type defaultTypeStruture = {name:string, url:string}
export type TAbilities = {ability:defaultTypeStruture, is_hidden:boolean, slot:number}[]
export type TTypes = {slot:number, type:defaultTypeStruture}[]
export type TSpecies = defaultTypeStruture
export type TStats = {base_stat:number, effort:number, stat:defaultTypeStruture}[]
